# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

before:
  hooks:
    - go mod tidy
    - go vet ./...

builds:
  - id: fibe
    main: ./cmd/fibe
    binary: fibe
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -X github.com/fibegg/sdk/internal/mcpserver.Version={{.Version}}

archives:
  - id: fibe
    formats: ["tar.gz"]
    format_overrides:
      - goos: windows
        formats: ["zip"]
    name_template: >-
      fibe_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: fibegg
    name: sdk
  draft: false
  prerelease: auto
  replace_existing_artifacts: true

homebrew_casks:
  - ids:
      - fibe
    name: fibe
    binaries:
      - fibe
    homepage: https://github.com/fibegg/sdk
    description: Official Go SDK, CLI, and MCP server for the Fibe platform
    url:
      verified: github.com/fibegg/sdk
    repository:
      owner: fibegg
      name: homebrew-sdk
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_msg_template: "chore(homebrew): update {{ .ProjectName }} to {{ .Tag }}"
    commit_author:
      name: github-actions[bot]
      email: 41898282+github-actions[bot]@users.noreply.github.com
    skip_upload: auto
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/fibe"]
          end
